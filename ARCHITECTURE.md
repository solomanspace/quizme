# QuizMe - Deploy Arxitektura

## 🏗️ Deployment Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        DEVELOPMENT                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Local Machine                                                   │
│  ┌──────────────┐              ┌──────────────┐                │
│  │  Frontend    │              │   Backend    │                │
│  │  React+Vite  │  ───────────▶│  Node+Express│                │
│  │  localhost:  │  API Calls   │  localhost:  │                │
│  │    3000      │              │    5000      │                │
│  └──────────────┘              └──────────────┘                │
│         │                              │                         │
│         │                              │                         │
│         └──────────┬───────────────────┘                         │
│                    │                                             │
│                    ▼                                             │
│            ┌──────────────┐                                      │
│            │    GitHub    │                                      │
│            │  Repository  │                                      │
│            └──────────────┘                                      │
└─────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│                        PRODUCTION                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                     USER BROWSER                          │  │
│  │  https://quizme.netlify.app                              │  │
│  └──────────────────────────────────────────────────────────┘  │
│         │                                                        │
│         │ HTTPS Request                                         │
│         ▼                                                        │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    NETLIFY CDN                            │  │
│  │  ┌──────────────────────────────────────────────┐        │  │
│  │  │  Frontend - React SPA                        │        │  │
│  │  │  - HTML, CSS, JavaScript                     │        │  │
│  │  │  - Static Assets                             │        │  │
│  │  │  - Auto HTTPS                                │        │  │
│  │  │  - Global CDN                                │        │  │
│  │  └──────────────────────────────────────────────┘        │  │
│  └──────────────────────────────────────────────────────────┘  │
│         │                                                        │
│         │ API Call (axios.post)                                 │
│         │ https://quizme-backend.onrender.com/api/upload        │
│         ▼                                                        │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                   RENDER SERVER                           │  │
│  │  ┌──────────────────────────────────────────────┐        │  │
│  │  │  Backend - Express API                       │        │  │
│  │  │  - File Upload (Multer)                      │        │  │
│  │  │  - File Processing                           │        │  │
│  │  │  - Question Extraction                       │        │  │
│  │  │  - CORS Enabled                              │        │  │
│  │  └──────────────────────────────────────────────┘        │  │
│  └──────────────────────────────────────────────────────────┘  │
│         │                                                        │
│         │ JSON Response                                         │
│         ▼                                                        │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                   NETLIFY CDN                             │  │
│  │  Display Results to User                                  │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│                      CI/CD PIPELINE                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Developer                                                       │
│      │                                                           │
│      │ git push                                                  │
│      ▼                                                           │
│  ┌──────────┐                                                   │
│  │  GitHub  │                                                   │
│  └──────────┘                                                   │
│      │                                                           │
│      ├───────── Webhook ──────────▶ ┌─────────────┐            │
│      │                              │   Netlify   │            │
│      │                              │   1. Build  │            │
│      │                              │   2. Deploy │            │
│      │                              │   3. Live!  │            │
│      │                              └─────────────┘            │
│      │                                                           │
│      └───────── Webhook ──────────▶ ┌─────────────┐            │
│                                     │   Render    │            │
│                                     │   1. Build  │            │
│                                     │   2. Deploy │            │
│                                     │   3. Live!  │            │
│                                     └─────────────┘            │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

## 📊 Data Flow

```
┌──────────┐
│   User   │
└────┬─────┘
     │
     │ 1. Upload File
     ▼
┌──────────────────┐
│  FileUpload.jsx  │
└────┬─────────────┘
     │
     │ 2. FormData + File
     ▼
┌──────────────────┐
│  Axios POST      │
│  to Backend API  │
└────┬─────────────┘
     │
     │ 3. HTTP Request
     ▼
┌──────────────────────────┐
│  Render Backend          │
│  ┌──────────────────┐   │
│  │  Multer          │   │
│  │  Save temp file  │   │
│  └────┬─────────────┘   │
│       │                  │
│       │ 4. Process       │
│       ▼                  │
│  ┌──────────────────┐   │
│  │  fileProcessor   │   │
│  │  - Parse file    │   │
│  │  - Extract Q&A   │   │
│  │  - Validate      │   │
│  └────┬─────────────┘   │
│       │                  │
│       │ 5. Return JSON   │
└───────┼──────────────────┘
        │
        │ 6. HTTP Response
        ▼
┌──────────────────┐
│  FileUpload.jsx  │
│  Receive data    │
└────┬─────────────┘
     │
     │ 7. Pass to parent
     ▼
┌──────────────────┐
│    App.jsx       │
│  Store questions │
└────┬─────────────┘
     │
     │ 8. Navigate
     ▼
┌──────────────────┐
│  QuizSetup.jsx   │
│  Configure test  │
└────┬─────────────┘
     │
     │ 9. Start quiz
     ▼
┌──────────────────┐
│  QuizTaking.jsx  │
│  - Shuffle Q&A   │
│  - Timer start   │
│  - User answers  │
└────┬─────────────┘
     │
     │ 10. Complete
     ▼
┌──────────────────┐
│  QuizResults.jsx │
│  - Calculate     │
│  - Display stats │
└──────────────────┘
```

## 🔄 Component Flow

```
App.jsx (Parent Component)
    │
    ├── step: 'upload'
    │   └── <FileUpload onFileProcessed={fn} />
    │       - Upload file
    │       - Process on backend
    │       - Return questions[]
    │
    ├── step: 'setup'
    │   └── <QuizSetup 
    │           totalQuestions={questions.length}
    │           onSetup={fn}
    │           onBack={fn} />
    │       - Select # of questions
    │       - Select time limit
    │       - Return config{}
    │
    ├── step: 'quiz'
    │   └── <QuizTaking
    │           questions={questions}
    │           config={config}
    │           onComplete={fn} />
    │       - Shuffle questions
    │       - Timer countdown
    │       - Collect answers
    │       - Return answers[]
    │
    └── step: 'results'
        └── <QuizResults
                questions={questions}
                answers={answers}
                config={config}
                onRestart={fn} />
            - Calculate stats
            - Display results
            - Filter options
```

## 🗂️ File Structure on Servers

### Netlify (Frontend)
```
/
├── index.html
├── assets/
│   ├── index-abc123.js      (minified)
│   └── index-def456.css     (minified)
└── _redirects               (SPA routing)
```

### Render (Backend)
```
/opt/render/project/src/
├── server.js
├── fileProcessor.js
├── package.json
├── node_modules/
└── uploads/                 (temporary, auto-cleaned)
```

## 🌍 Global Distribution

```
┌──────────────────────────────────────────────┐
│             Netlify Global CDN               │
├──────────────────────────────────────────────┤
│                                              │
│  🌍 North America    🌍 Europe               │
│  🌍 Asia             🌍 Australia            │
│  🌍 South America    🌍 Africa               │
│                                              │
│  → Fastest server serves user                │
│  → < 50ms latency                            │
│  → HTTPS everywhere                          │
│                                              │
└──────────────────────────────────────────────┘

┌──────────────────────────────────────────────┐
│           Render Backend Server              │
├──────────────────────────────────────────────┤
│                                              │
│  📍 Frankfurt (or selected region)           │
│                                              │
│  → Single server (free tier)                 │
│  → Sleeps after 15 min                       │
│  → 30-60s cold start                         │
│                                              │
└──────────────────────────────────────────────┘
```

## 📈 Scaling Strategy

### Current (Free Tier)
```
Users: ~100 concurrent
Bandwidth: 100GB/month (Netlify)
Backend: 512MB RAM (Render)
```

### Future (Paid Tier)
```
Users: ~1000+ concurrent
Bandwidth: Unlimited
Backend: 2GB+ RAM, Auto-scaling
Database: MongoDB/PostgreSQL
CDN: Global edge caching
```

## 🔒 Security Layers

```
User Request
    │
    ├─▶ HTTPS (TLS 1.3)
    │
    ├─▶ Netlify Firewall
    │   - DDoS protection
    │   - Rate limiting
    │
    ├─▶ CORS Headers
    │   - Allowed origins
    │
    ├─▶ File Validation
    │   - Type check
    │   - Size limit (10MB)
    │
    ├─▶ Content Sanitization
    │   - XSS protection
    │   - SQL injection prevention
    │
    └─▶ Secure Response
        - Security headers
        - No sensitive data exposure
```

---

Bu arxitektura QuizMe ning ishlab chiqish va production muhitlarini to'liq tasvirlaydi.
